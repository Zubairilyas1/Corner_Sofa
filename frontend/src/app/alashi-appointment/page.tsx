'use client';
import {useState} from 'react';
export default function AlashiAppointment(){const [name,setName]=useState('');const [contact,setContact]=useState('');const [date,setDate]=useState('');const [notice,setNotice]=useState('');const [busy,setBusy]=useState(false);return <section className="max-w-xl mx-auto px-5 py-20"><h1 className="text-3xl mb-4">Visit Corner Sofa UK</h1><p className="mb-6">Visits are by appointment only. Samiullah will confirm your preferred date and time.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);try{const r=await fetch('/api/alashi/appointment/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,contact,date:new Date(date).toISOString()})});const d=await r.json();if(!r.ok)throw new Error(d.error);setNotice(d.message);setName('');setContact('');setDate('');}catch(e){setNotice(e instanceof Error?e.message:'Please try again.');}finally{setBusy(false);}}}>{[['Name',name,setName],['Email or phone',contact,setContact]] .map(([label,value,setter])=><label key={String(label)} className="block mb-4">{String(label)}<input required value={String(value)} onChange={e=>(setter as (v:string)=>void)(e.target.value)} className="block w-full border rounded-xl p-3 mt-2"/></label>)}<label className="block mb-5">Preferred date/time (your local time)<input required type="datetime-local" value={date} onChange={e=>setDate(e.target.value)} className="block w-full border rounded-xl p-3 mt-2"/></label><button disabled={busy} className="bg-[#263b30] text-white rounded-xl px-6 py-3">Request appointment</button><p role="status" className="mt-5">{notice}</p></form></section>;}
